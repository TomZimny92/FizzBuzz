// classic fizzbuzz
//
//
let fizzMap = new Map<number, string>([
	[3, "Fizz"],
	[5, "Buzz"]
])
function fizzbuzz(n: number):string[] {
	let result: string[] = []
	let mod: number = 0
	for (let i = 1; i <= n.length; ++i){
		mod = 4
		switch(mod){
			case 
		}
	}
}
